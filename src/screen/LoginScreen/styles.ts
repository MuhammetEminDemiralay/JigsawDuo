import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center'
    },
    signingBtn: {

    },
    loginText: {
        fontSize: 30,
        fontWeight: '700',
        color: '#000814',
        marginBottom: 30,
        letterSpacing: 2,
        position: 'absolute',
        top: '25%'
    }
})